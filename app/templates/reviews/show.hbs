<hr>

<div class="panel">
  <p>"{{model.title}}" by user <b>{{model.user_id}}</b></p>
  <p>{{model.body}}</p>
  <p>
    {{#link-to "reviews.edit" model.id class="button"}}Edit{{/link-to}}
    <button {{action "deleteReview" this}} class="submit">Delete</button>
  </p>
</div>
