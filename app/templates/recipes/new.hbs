<hr>

<div>
  <p>{{input type="text" placeholder="Title" value=title}}</p>
  <p>{{textarea type="text" placeholder="My recipe starts with..." value=body rows="4" cols="50"}}</p>
  <button {{action "createRecipe"}} class="submit">Create</button>
</div>
