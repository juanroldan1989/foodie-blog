<hr>

<div class="panel">
  <p>"{{model.title}}" by user <b>{{model.user_id}}</b></p>
  <p>{{model.body}}</p>
  <p>
    {{#link-to "recipes.edit" model.id class="button"}}Edit{{/link-to}}
    <button {{action "deleteRecipe" this}} class="submit">Delete</button>
  </p>
</div>
